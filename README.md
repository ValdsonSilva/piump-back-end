# Piump-Back-End

Minimal Node+TS+Express backend for booking + Stripe subscriptions + Google
Sheets + basic admin.

## 1) Install & Run

```bash
pnpm i # or npm i / yarn
cp .env.example .env
pnpm dev
